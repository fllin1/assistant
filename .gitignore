# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.envrc

# Secrets — real keys never committed; .env.example is the template.
.env
.env.local

# Claude Code local settings (personal, not committed)
.claude/settings.local.json

# Obsidian vault metadata is personal; Markdown docs can be tracked.
.obsidian/

# Local agent / OMX tooling installs
.agents/
.claude/skills/
.codex/
skills-lock.json

# Local OMC runtime state. Commit durable plan artifacts under .omc/plans/
# only when they are intentionally part of the branch handoff.
.omc/state/
.omc/sessions/
.omc/project-memory.json

.DS_Store
